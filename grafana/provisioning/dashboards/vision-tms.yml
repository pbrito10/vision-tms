apiVersion: 1

providers:
  - name: Vision TMS
    orgId: 1
    folder: Vision TMS
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
